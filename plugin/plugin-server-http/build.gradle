apply plugin: 'org.jetbrains.kotlin.plugin.serialization'

dependencies {

	api project(':plugin')
	api project(':server:server-http')

}

processResources {
	from("$rootDir/frontend/public") {
		into("net/tassia/pancake/plugin/http/static")
	}
}
